import 'package:flutter/material.dart';
import '../widgets/buy_sell_widget.dart';

class BuySellScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: BuySellWidget(),
    );
  }
}
